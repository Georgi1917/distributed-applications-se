spring:
  application:
    name: JobListing

  datasource:
    url: jdbc:postgresql://localhost:5432/JobListingDB
    username: postgres
    password: password

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true
    properties:
      hibernate:
        format_sql: true
